{# app/Resources/views/upload/upload.html.twig #}
{% extends 'base.html.twig' %}

{% set page_title = 'upload.title.upload'|trans %}

{% block body %}

	<h1>{{ page_title }}</h1>

	{{ form_start(form) }}
	{{ form_widget(form) }}
	<input type="submit" value="{{ 'upload.label.upload'|trans }}">
	{{ form_end(form) }}


{% endblock %}